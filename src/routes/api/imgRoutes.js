////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
const express = require('express');
const router = express.Router();
const imgController = require('../../controllers/api/imgController');

router.get('/userImg', imgController.userImg);
router.get('/userImg/:id', imgController.userImg);
router.get('/productImg/:id', imgController.productImg);

module.exports = router;
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
